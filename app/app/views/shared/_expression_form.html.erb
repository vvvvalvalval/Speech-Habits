<%= form_for @expression do |f| %>
<%= @teacher.id %>

  <%= f.hidden_field :teacher_id, :value => @teacher.id %>
 
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Soumettre" %> 
  </div>
<% end %>