<h1>
  <%= @user.name %>
</h1>

<p>
	<% if current_user == @user%> 
		Vous avez 
	<% else %> 
		<%= @user.name %> a 
	<%end%> 
	repéré <%= Event.count :conditions => { :user_id => @user.id } %> expressions
</p>

<p> 
	<%= link_to "Leaderboard", leaderboard_path %>
</p>